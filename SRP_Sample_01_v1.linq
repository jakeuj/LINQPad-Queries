<Query Kind="Statements" />

public class OrderManger
{
	public bool LoadOrder()
	{
		// 1. 建立資料庫連線 (包含寫死的連接字串)
		// 2. 執行 ADO.NET 資料存取 (包含資料篩選)
		// 3. 跑回圈取得資料 (包含資料格式轉換)
		// 4. 回傳資料
		return true;
	}
}